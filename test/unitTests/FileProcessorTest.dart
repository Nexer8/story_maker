import 'package:flutter_test/flutter_test.dart';
import 'package:storymaker/services/FileProcessor.dart';

void main() {
  group('FileProcessor tests', () {
    final FileProcessor fileProcessor = FileProcessor();

    group('FileProcessor trim', () {
      test('it_FileProcessor_trim_default', () async {
        expect(1, 1);
      });
    });

    group('FileProcessor getDuration', () {
      test('it_FileProcessor_getDuration_default', () async {
        expect(1, 1);
      });
    });
  });
}
