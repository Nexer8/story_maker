import 'package:flutter_test/flutter_test.dart';
import 'package:storymaker/services/AudioProcessor.dart';

void main() {
  group('AudioProcessor tests', () {
    final AudioProcessor audioProcessor = AudioProcessor();

    group('AudioProcessor trim', () {
      test('it_AudioProcessor_trim_default', () async {
        expect(1, 1);
      });
    });

    group('AudioProcessor getDuration', () {
      test('it_AudioProcessor_getDuration_default', () async {
        expect(1, 1);
      });
    });
  });
}
